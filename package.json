{
  "private": true,
  "scripts": {
    "format": "prettier --ignore-path .gitignore --write . --cache"
  },
  "devDependencies": {
    "prettier": "latest",
    "prettier-plugin-svelte": "latest",
    "svelte": "latest"
  },
  "prettier": {
    "printWidth": 120
  }
}
