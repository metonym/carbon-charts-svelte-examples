{
  "private": true,
  "scripts": {
    "format": "prettier --ignore-path .gitignore --write ."
  },
  "devDependencies": {
    "prettier": "^3.1.0",
    "prettier-plugin-svelte": "^3.1.2",
    "svelte": "^4.2.8"
  },
  "prettier": {
    "printWidth": 120
  }
}
