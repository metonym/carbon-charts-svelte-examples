{
  "private": true,
  "scripts": {
    "format": "prettier --ignore-path .gitignore --write ."
  },
  "devDependencies": {
    "prettier": "^3.0.0",
    "prettier-plugin-svelte": "^3.0.0",
    "svelte": "^4.1.0"
  },
  "prettier": {
    "printWidth": 120
  }
}
