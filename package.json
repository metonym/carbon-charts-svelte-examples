{
  "private": true,
  "scripts": {
    "format": "prettier --write . --cache",
    "upgrade-examples": "bun upgrade-examples.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "latest",
    "prettier-plugin-svelte": "latest",
    "svelte": "latest"
  },
  "prettier": {
    "plugins": [
      "prettier-plugin-svelte"
    ],
    "printWidth": 120
  }
}
